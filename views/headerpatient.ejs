<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عيادتي - الصفحة الرئيسية</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Reset & Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Tajawal', sans-serif;
        }
        
        body {
            background-color: #f8fafc;
            color: #1e293b;
            line-height: 1.6;
        }
        
        /* Header Styles */
        .header {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            color: white;
            padding: 1.5rem 0;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .logo i {
            color: #93c5fd;
        }
        
        /* Navigation */
        .nav-menu {
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }
        
        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .nav-item i {
            font-size: 1.2rem;
            margin-bottom: 0.3rem;
        }
        
        .nav-item:hover {
            transform: translateY(-3px);
            opacity: 0.9;
        }
        
        .nav-link {
            color: white;
            text-decoration: none;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        /* Patient Dropdown */
        .patient-dropdown {
            position: relative;
            display: inline-block;
        }
        
        .patient-name {
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .patient-menu {
            display: none;
            position: absolute;
            background-color: white;
            min-width: 200px;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            z-index: 100;
            right: 0;
            border-radius: 0.5rem;
            overflow: hidden;
        }
        
        .patient-menu-header {
            padding: 1rem;
            border-bottom: 1px solid #e2e8f0;
            background-color: #3b82f6;
            color: white;
        }
        
        .patient-menu-item {
            color: #1e293b;
            padding: 0.8rem 1rem;
            text-decoration: none;
            display: block;
            transition: background-color 0.3s;
        }
        
        .patient-menu-item:hover {
            background-color: #f1f5f9;
        }
        
        .patient-menu-divider {
            border-top: 1px solid #e2e8f0;
        }
        
        /* Hero Section */
        .hero {
            background: linear-gradient(rgba(30, 41, 59, 0.85), rgba(30, 41, 59, 0.85)), 
                        url('/images/medical-bg.jpg') center/cover;
            height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            padding: 0 1rem;
        }
        
        .hero-content {
            max-width: 800px;
        }
        
        .hero h1 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }
        
        .hero p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        
        .btn {
            display: inline-block;
            padding: 0.8rem 2rem;
            border-radius: 0.375rem;
            font-weight: 600;
            transition: all 0.3s;
            text-decoration: none;
        }
        
        .btn-primary {
            background-color: #f59e0b;
            color: #1e293b;
        }
        
        .btn-primary:hover {
            background-color: #fbbf24;
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }
        
        /* Doctors Section */
        .doctors-section {
            padding: 4rem 1rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 3rem;
            position: relative;
        }
        
        .section-title h2 {
            font-size: 2rem;
            color: #1e293b;
            display: inline-block;
            padding-bottom: 0.5rem;
        }
        
        .section-title h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background-color: #3b82f6;
        }
        
        /* Doctors Grid */
        .doctors-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
        }
        
        .doctor-card {
            background: white;
            border-radius: 0.5rem;
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            transition: all 0.3s;
        }
        
        .doctor-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }
        
        .doctor-img-container {
            height: 250px;
            overflow: hidden;
            position: relative;
        }
        
        .doctor-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }
        
        .doctor-card:hover .doctor-img {
            transform: scale(1.05);
        }
        
        .doctor-info {
            padding: 1.5rem;
            text-align: center;
        }
        
        .doctor-name {
            font-size: 1.3rem;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }
        
        .doctor-specialty {
            color: #64748b;
            margin-bottom: 1rem;
            font-size: 0.95rem;
        }
        
        .btn-book {
            background-color: #3b82f6;
            color: white;
            padding: 0.6rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: all 0.3s;
            display: inline-block;
        }
        
        .btn-book:hover {
            background-color: #2563eb;
        }
        
        /* Features Section */
        .features {
            background-color: #f1f5f9;
            padding: 4rem 1rem;
        }
        
        .features-container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }
        
        .feature-card {
            background: white;
            padding: 2rem;
            border-radius: 0.5rem;
            text-align: center;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            transition: all 0.3s;
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
        }
        
        .feature-icon {
            font-size: 2.5rem;
            color: #3b82f6;
            margin-bottom: 1.5rem;
        }
        
        .feature-title {
            font-size: 1.2rem;
            color: #1e293b;
            margin-bottom: 1rem;
        }
        
        /* Footer */
        .footer {
            background-color: #1e293b;
            color: #e2e8f0;
            padding: 3rem 1rem;
            text-align: center;
        }
        
        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
        }
        
        .footer-link {
            color: #93c5fd;
            text-decoration: none;
            transition: all 0.3s;
        }
        
        .footer-link:hover {
            color: #3b82f6;
        }
        
        .copyright {
            margin-top: 2rem;
            color: #94a3b8;
            font-size: 0.9rem;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column;
                gap: 1rem;
            }
            
            .nav-menu {
                width: 100%;
                justify-content: space-around;
                flex-wrap: wrap;
            }
            
            .hero h1 {
                font-size: 2rem;
            }
            
            .hero p {
                font-size: 1rem;
            }
            
            .doctors-grid {
                grid-template-columns: 1fr;
            }
            
            .patient-menu {
                right: -50%;
            }
        }
        
        /* Loading & Error States */
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.9);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            flex-direction: column;
        }
        
        .spinner {
            font-size: 3rem;
            color: #3b82f6;
            animation: spin 1s linear infinite;
            margin-bottom: 1rem;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .error-message {
            background-color: #fee2e2;
            color: #b91c1c;
            padding: 1rem;
            border-radius: 0.375rem;
            margin: 1rem auto;
            max-width: 800px;
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .error-icon {
            font-size: 1.5rem;
        }
        
        .empty-state {
            text-align: center;
            padding: 2rem;
            color: #64748b;
        }
    </style>
</head>
<body>
    
    <!-- Header Section -->
    <header class="header">
        <div class="header-container">
            <div class="logo">
                <i class="fas fa-heartbeat"></i>
                <span>عيادتي</span>
            </div>
            <nav class="nav-menu">
                <div class="nav-item">
                    <a href="/" class="nav-link">
                        <i class="fas fa-home"></i>
                        <span>الرئيسية</span>
                    </a>
                </div>
                <div class="nav-item" onclick="window.location.href='<%= patient ? '/patient/sessions' : '/signUp' %>'">
                    <i class="fas fa-calendar-alt"></i>
                    <span>جلساتي</span>
                </div>
                <div class="nav-item" onclick="window.location.href='<%= patient ? '/patient/sessions' : '/signUp' %>'">
                    <i class="fas fa-history"></i>
                    <span>مراجعاتي</span>
                </div>
                <div class="nav-item" onclick="window.location.href='<%= patient ? '/request-session' : '/signUp' %>'">
                    <i class="fas fa-user-md"></i>
                    <span>الأطباء</span>
                </div>
                
                <% if (patient) { %>
                    <div class="patient-dropdown">
                        <div class="nav-item patient-name" onclick="togglePatientMenu()">
                            <i class="fas fa-user-circle"></i>
                            <span><%= patient.name %></span>
                        </div>
                        <div id="patientMenu" class="patient-menu">
                            <div class="patient-menu-header">
                                <%= patient.name %>
                            </div>
                            <a href="/profile" class="patient-menu-item">
                                <i class="fas fa-user" style="margin-left: 8px;"></i>
                                الملف الشخصي
                            </a>
                            <a href="/wallet" class="patient-menu-item">
                                <i class="fas fa-wallet" style="margin-left: 8px;"></i>
                                المحفظة: <%= patient.wallet.balance %> ر.س
                            </a>
                            <div class="patient-menu-divider"></div>
                            <a href="/logout" class="patient-menu-item">
                                <i class="fas fa-sign-out-alt" style="margin-left: 8px;"></i>
                                تسجيل الخروج
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <div class="nav-item" onclick="window.location.href='/login'">
                        <i class="fas fa-sign-in-alt"></i>
                        <span>تسجيل الدخول</span>
                    </div>
                <% } %>
            </nav>
        </div>
    </header>
    <script>
        function togglePatientMenu() {
            const menu = document.getElementById('patientMenu');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }

        // Close the dropdown if clicked outside
        window.onclick = function(event) {
            if (!event.target.matches('.patient-name') && !event.target.closest('.patient-dropdown')) {
                const dropdowns = document.getElementsByClassName("patient-menu");
                for (let i = 0; i < dropdowns.length; i++) {
                    const openDropdown = dropdowns[i];
                    if (openDropdown.style.display === 'block') {
                        openDropdown.style.display = 'none';
                    }
                }
            }
        }
    </script>