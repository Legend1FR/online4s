<!-- node-proje
 t\views\headeradmin.ejs file -->
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة الإدارة - عيادتي</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0; padding: 0; box-sizing: border-box;
            font-family: 'Tajawal', sans-serif;
        }
        body {
            background-color: #f8fafc;
            color: #1e293b;
            line-height: 1.6;
        }
        .header {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            color: white;
            padding: 1.5rem 0;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-direction: row-reverse;
        }
        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        .logo i {
            color: #93c5fd;
        }
        .nav-menu {
            display: flex;
            gap: 1.5rem;
            align-items: center;
            justify-content: flex-end;
        }
        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        .nav-item i {
            font-size: 1.2rem;
            margin-bottom: 0.3rem;
        }
        .nav-item:hover {
            transform: translateY(-3px);
            opacity: 0.9;
        }
        .nav-link {
            color: white;
            text-decoration: none;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .admin-dropdown {
            position: relative;
            display: inline-block;
        }
        .admin-name {
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        .admin-menu {
            display: none;
            position: absolute;
            background-color: white;
            min-width: 200px;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            z-index: 100;
            right: 0;
            left: auto;
            text-align: right;
            border-radius: 0.5rem;
            overflow: hidden;
        }
        .admin-menu-header {
            padding: 1rem;
            border-bottom: 1px solid #e2e8f0;
            background-color: #3b82f6;
            color: white;
        }
        .admin-menu-item {
            color: #1e293b;
            padding: 0.8rem 1rem;
            text-decoration: none;
            display: block;
            transition: background-color 0.3s;
        }
        .admin-menu-item:hover {
            background-color: #f1f5f9;
        }
        .admin-menu-divider {
            border-top: 1px solid #e2e8f0;
        }
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column-reverse;
            }
            .nav-menu {
                width: 100%;
                justify-content: flex-end;
                flex-wrap: wrap;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="header-container">
            <div class="logo">
                <i class="fas fa-heartbeat"></i>
                <span>عيادتي</span>
            </div>
            <nav class="nav-menu">
                <div class="nav-item">
                    <a href="/admin/dashboard" class="nav-link">
                        <i class="fas fa-tachometer-alt"></i>
                        <span>لوحة التحكم</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="/admin/patients-management" class="nav-link">
                        <i class="fas fa-user-injured"></i>
                        <span>إدارة المرضى</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="/admin/doctors-management" class="nav-link">
                        <i class="fas fa-user-md"></i>
                        <span>إدارة الأطباء</span>
                    </a>
                </div>
                <div class="nav-item">
                    <a href="/admin/payments-management" class="nav-link">
                        <i class="fas fa-wallet"></i>
                        <span>إدارة المدفوعات</span>
                    </a>
                </div>
                <div class="admin-dropdown">
                    <div class="nav-item admin-name" onclick="toggleAdminMenu()" tabindex="0">
                        <i class="fas fa-user-circle"></i>
                        <span>
                            <% if (typeof admin !== 'undefined' && admin) { %>
                                <%= admin.username %>
                            <% } else { %>
                                غير مسجل الدخول
                            <% } %>
                        </span>
                        
                    </div>
                    <div id="adminMenu" class="admin-menu">
                        <div class="admin-menu-header">
                            <% if (typeof admin !== 'undefined' && admin) { %>
                                <%= admin.username %>
                            <% } else { %>
                                غير مسجل الدخول
                            <% } %>
                        </div>
                        <a href="/admin/settings" class="admin-menu-item">
                            <i class="fas fa-cogs" style="margin-left: 8px;"></i>
                            الإعدادات
                        </a>
                        <div class="admin-menu-divider"></div>
                        <a href="/admin/logout" class="admin-menu-item">
                            <i class="fas fa-sign-out-alt" style="margin-left: 8px;"></i>
                            تسجيل الخروج
                        </a>
                    </div>
                </div>
            </nav>
        </div>
    </header>
    <script>
        function toggleAdminMenu() {
            const menu = document.getElementById('adminMenu');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }
        // Close the dropdown if clicked outside
        document.addEventListener('click', function(event) {
            const dropdown = document.querySelector('.admin-dropdown');
            const menu = document.getElementById('adminMenu');
            if (menu && !dropdown.contains(event.target)) {
                menu.style.display = 'none';
            }
        });
    </script>
</body>
</html>
